services:
  nginx:
    build: ./requirements/nginx/
    ports:
      - 80:80
      - 443:443
    volumes:
      - /home/artmende/data:/somefolder

# networks:

# volumes:
#   www:
#     data: /home/artmende/data
